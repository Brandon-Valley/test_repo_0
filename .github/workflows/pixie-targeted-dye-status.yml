name: Pixie targeted dye status

on:
  issue_comment:
    types: [created]

permissions:
  actions: read
  contents: write

jobs:
  status:
    if: github.event.issue.number == 6 && contains(github.event.comment.body, 'check pixie targeted dye research')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p status
          runs=$(gh api "repos/${GITHUB_REPOSITORY}/actions/workflows/pixie-dye-targeted-research.yml/runs?per_page=20")
          run=$(jq '[.workflow_runs[] | select(.conclusion != "skipped")][0]' <<<"$runs")
          id=$(jq -r .id <<<"$run")
          jobs=$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${id}/jobs?per_page=100" | jq '[.jobs[] | {id,name,status,conclusion,steps:[.steps[]? | {name,status,conclusion}]}]')
          jq -n --argjson run "$run" --argjson jobs "$jobs" '{run:{id:$run.id,status:$run.status,conclusion:$run.conclusion,html_url:$run.html_url},jobs:$jobs}' > status/pixie-targeted-dye-status.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add status/pixie-targeted-dye-status.json
          git commit -m 'Update targeted dye research status' || exit 0
          git push
