name: Report Pixie wearable placement JSON v2 status

on:
  issue_comment:
    types: [created]

permissions:
  actions: read
  contents: write

jobs:
  report:
    if: contains(github.event.comment.body, 'report pixie wearable placement json v2')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const runs=await github.rest.actions.listWorkflowRuns({owner:context.repo.owner,repo:context.repo.repo,workflow_id:'pixie-wearable-placement-json-v2.yml',per_page:5});
            const payload={generated_at:new Date().toISOString(),runs:runs.data.workflow_runs.map(r=>({id:r.id,status:r.status,conclusion:r.conclusion,created_at:r.created_at,updated_at:r.updated_at,html_url:r.html_url}))};
            const path='status/pixie-wearable-placement-json-v2.json';let sha;
            try{const old=await github.rest.repos.getContent({owner:context.repo.owner,repo:context.repo.repo,path});sha=old.data.sha;}catch(e){if(e.status!==404)throw e;}
            await github.rest.repos.createOrUpdateFileContents({owner:context.repo.owner,repo:context.repo.repo,path,message:'Update wearable placement JSON v2 status',content:Buffer.from(JSON.stringify(payload,null,2)+'\n').toString('base64'),sha});
